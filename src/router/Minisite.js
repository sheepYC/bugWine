import {Route} from 'react-router-dom';
import React from 'react';
import Minisite from '../Components/ShopCar/'

const RouterMinisite = (props)=>{
		return (
		<Minisite {...props}>

		</Minisite>
	)
}

export default RouterMinisite